parameters:
    app.default_admin_password_hash: '$2y$12$4L1U3009ffceyxtaCcoh2efQx.XeY48u6iStryuZJsIfrOUGPjQp.'

security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        admin_users:
            memory:
                users:
                    admin:
                        password: '%env(default:app.default_admin_password_hash:ADMIN_PASSWORD_HASH)%'
                        roles: ['ROLE_SUPER_ADMIN']

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build|bundles)/
            security: false

        api:
            pattern: ^/api
            security: false

        main:
            lazy: true
            provider: admin_users
            form_login:
                login_path: admin_login
                check_path: admin_login
                default_target_path: admin
                enable_csrf: true
            logout:
                path: admin_logout
                target: admin_login

    access_control:
        - { path: ^/admin/login, roles: PUBLIC_ACCESS }
        - { path: ^/admin/logout, roles: PUBLIC_ACCESS }
        - { path: ^/admin, roles: ROLE_SUPER_ADMIN }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
